# TaskInterferenceSimulator
multi agent timed discrete event simulator to simulate the interference between tasks accessing shared resources (like memory)


# Installation:

 ```
   npm install js-simulator
 ```
